---
type: 测试用例
stage: active
feature: JSON数据预处理解析器
version: "1.0"

owner: sea
created: 2026-02-08
updated: 2026-02-08
---

# 测试用例: JSON数据预处理解析器

## 关联文档

| 文档 | 路径 |
|------|------|
| PRD | [[docs/产品/PRD/PRD.md]] |
| 源代码 | [[scripts/preprocessor.py]] |

---

## 用例概览

| 用例ID | 名称 | 优先级 | 状态 |
|--------|------|--------|------|
| TC-PRE-001 | 群组信息解析验证 | P0 | ✅ 通过 |
| TC-PRE-002 | 成员列表提取验证 | P0 | ✅ 通过 |
| TC-PRE-003 | 成员消息正确归类验证 | P0 | ✅ 通过 |
| TC-PRE-004 | 系统消息(type=80)过滤验证 | P0 | ✅ 通过 |
| TC-PRE-005 | 水消息(type=5)过滤验证 | P1 | ✅ 通过 |
| TC-PRE-006 | 基于系统消息的入群时间提取 | P1 | ✅ 通过 |
| TC-PRE-007 | 媒体消息(图片)过滤验证 | P1 | ✅ 通过 |
| TC-PRE-008 | 时间范围过滤功能验证 | P1 | ⏳ 待实现 (xfail) |
| TC-PRE-009 | 空文件/不存在文件异常处理 | P2 | ✅ 通过 |
| TC-PRE-010 | 损坏的JSON格式异常处理 | P2 | ✅ 通过 |
| TC-PRE-011 | 特殊字符与Emoji解析验证 | P2 | ✅ 通过 |

---

## P0 用例（必须通过）

### TC-PRE-001: 群组信息解析验证

| 项目 | 内容 |
|------|------|
| 优先级 | P0 |
| 关联功能 | 提取群组信息 |
| 测试类型 | 功能测试 |

**测试步骤**:
1. 调用 `preprocess_chat_data` 传入 `mock_chat_data.json`。
2. 验证返回对象的 `group_info` 字段。

**预期结果**:
- `name` 等于 "测试群聊"
- `group_id` 等于 "123456789@chatroom"
- `platform` 等于 "wechat"

**实际结果**: ✅ 通过

---

### TC-PRE-002: 成员列表提取验证

| 项目 | 内容 |
|------|------|
| 优先级 | P0 |
| 关联功能 | 提取成员列表 |
| 测试类型 | 功能测试 |

**测试步骤**:
1. 解析 `mock_chat_data.json`。
2. 检查结果中的 `members` 字典。

**预期结果**:
- 字典包含 "user_01" 和 "user_02" 两个键。
- 成员昵称分别为 "张三" 和 "李四"。

**实际结果**: ✅ 通过

---

### TC-PRE-003: 成员消息正确归类验证

| 项目 | 内容 |
|------|------|
| 优先级 | P0 |
| 关联功能 | 提取消息列表 |
| 测试类型 | 功能测试 |

**测试步骤**:
1. 解析包含多位成员消息的 JSON。
2. 分别检查每个成员对象下的 `messages` 列表。

**预期结果**:
- `user_01` 的消息列表中只包含其发送的消息。
- 消息包含 `id`, `timestamp`, `msg_type`, `content` 四个核心字段。

**实际结果**: ✅ 通过

---

### TC-PRE-004: 系统消息(type=80)过滤验证

| 项目 | 内容 |
|------|------|
| 优先级 | P0 |
| 关联功能 | 过滤系统消息 |
| 测试类型 | 负面测试 |

**前置条件**:
- 输入数据中包含 `type: 80` 的记录（如“加入群聊”）。

**测试步骤**:
1. 执行解析流程。
2. 遍历结果中所有成员的消息列表。

**预期结果**:
- 任何成员的消息列表中均不含 `msg_type: 80` 的消息。
- 统计信息 `filteredMessages` 计数正确。

**实际结果**: ✅ 通过

---

## P1 用例（应该通过）

### TC-PRE-005: 水消息(type=5)过滤验证

| 项目 | 内容 |
|------|------|
| 优先级 | P1 |
| 关联功能 | 过滤无效内容 |
| 测试类型 | 功能测试 |

**前置条件**:
- 输入数据包含动画表情（type: 5）。

**测试步骤**:
1. 执行解析流程。
2. 检查输出消息列表。

**预期结果**:
- 动画表情消息被过滤，不出现在最终结果中。

**实际结果**: ✅ 通过

---

### TC-PRE-006: 基于系统消息的入群时间提取

| 项目 | 内容 |
|------|------|
| 优先级 | P1 |
| 关联功能 | 提取入群时间 |
| 测试类型 | 逻辑测试 |

**测试步骤**:
1. 输入包含 `“李四”加入群聊` 且 type 为 80 的消息。
2. 检查 `members["user_02"].join_time`。

**预期结果**:
- 成功解析出时间并转换为 ISO 格式。
- 李四的 `join_time` 应为 `2024-02-03T22:40:00Z`。

**实际结果**: ✅ 通过

---

### TC-PRE-007: 媒体消息(图片)过滤验证

| 项目 | 内容 |
|------|------|
| 优先级 | P1 |
| 关联功能 | 隐私与数据精简 |
| 测试类型 | 负面测试 |

**测试步骤**:
1. 调用 `is_valid_message(1, include_media=True)`。
2. 调用 `is_valid_message(1, include_media=False)`。

**预期结果**:
- 两次调用均返回 `False`（符合业务需求，图片必须过滤）。

**实际结果**: ✅ 通过

---

### TC-PRE-008: 时间范围过滤功能验证

| 项目 | 内容 |
|------|------|
| 优先级 | P1 |
| 关联功能 | 支持时间范围过滤 |
| 测试类型 | 功能测试 |

**预期结果**:
- 传入 `startTime` 和 `endTime` 后，仅输出该范围内的消息。

**实际结果**: ⏳ 待实现 (xfail)
**备注**: 当前脚本代码尚未实现过滤参数。

---

## P2 用例（可以通过）

### TC-PRE-009: 空文件/不存在文件异常处理

| 项目 | 内容 |
|------|------|
| 优先级 | P2 |
| 关联功能 | 健壮性 |
| 测试类型 | 异常测试 |

**测试步骤**:
1. 传入一个不存在的文件路径给解析器。

**预期结果**:
- 程序抛出 `FileNotFoundError` 而非发生逻辑错误。

**实际结果**: ✅ 通过

---

### TC-PRE-010: 损坏的JSON格式异常处理

| 项目 | 内容 |
|------|------|
| 优先级 | P2 |
| 关联功能 | 健壮性 |
| 测试类型 | 异常测试 |

**测试步骤**:
1. 传入一个内容截断或语法错误的 `.json` 文件。

**预期结果**:
- 程序抛出 `json.JSONDecodeError`。

**实际结果**: ✅ 通过

---

### TC-PRE-011: 特殊字符与Emoji解析验证

| 项目 | 内容 |
|------|------|
| 优先级 | P2 |
| 关联功能 | 稳健性 |
| 测试类型 | 兼容性测试 |

**测试数据**:
```json
{
  "meta": {"name": "🌟Emoji群🌟"},
  "members": [{"platformId": "u1", "accountName": "李' OR 1=1 --"}],
  "messages": [{"type": 0, "content": "🔥\n换行符"}]
}
```

**预期结果**:
- 解析正常，字符不乱码，Emoji 完整保留。

**实际结果**: ✅ 通过

---

## 执行记录

| 日期 | 执行人 | 通过 | 失败 | 阻塞 | 备注 |
|------|--------|------|------|------|------|
| 2026-02-08 |  | 10 | 0 | 1 | TC-008 待开发实现 |

---

**最后更新**: 2026-02-08
